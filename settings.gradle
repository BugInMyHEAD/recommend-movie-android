dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Recommend Movie"

include ':app'
include ':domain'
include ':data'
include ':library-di'
include ':home'
